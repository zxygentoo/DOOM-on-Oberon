; The emulator submodule is its own dune project (private library behind its own
; dune-project boundary). Treat it as raw data here; its lib/ sources are compiled
; INTO this project by vendor/emu (copy_files), mirroring the host repo's `oracle`.
; This leaves the submodule pristine.

(data_only_dirs oberon-risc-emu-ocaml)
