; doomcc — the whole-program driver (AGENT.md 1b): preprocessed C (.i) -> RISC5 blob.
; The shape of the full pipeline: front (parse + amalgamate) and per-function codegen are
; real; the instr-level linker + blob emit (ABI §6 / 3b) are stubbed. See bin/doomcc.ml.

(executable
 (name doomcc)
 (libraries backend goblint-cil emu))
