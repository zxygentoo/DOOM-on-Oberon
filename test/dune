; Backend bring-up + the slice-1 differential jig.
;   test_runner  : hand-built instr -> encode -> run (Backend.Runner plumbing proof, slice 0)
;   test_codegen : compile small C leaves with OUR backend, run in the emulator, diff vs host gcc
(tests
 (names test_runner test_codegen)
 (libraries emu backend unix))
